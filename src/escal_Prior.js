function EscalonadorPrioridade (){    this.filaProcessos;                this.construtor = function ()    {        this.filaProcessos = [];    };                this.empty = function ()    {        if (this.filaProcessos.length == 0)            return true;        else            return false;    };                this.addProcesso = function (p)    {        this.filaProcessos.splice(0, 0, p);        this.filaProcessos.sort ( function (a, b)        {            return a.prioridade < b.prioridade;        });    };            this.top = function ()    {        var n = this.filaProcessos.length;        return this.filaProcessos[n-1];    }    this.pickProcesso = function ()    {        return this.filaProcessos.pop();    };                this.addEspera = function (n)    {        for (i in this.filaProcessos)        {            for (var j=0; j<n; j++)                this.filaProcessos[i].incrementaTEspera();        }    };                this.construtor();}